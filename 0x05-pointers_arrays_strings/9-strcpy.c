#include <stdio.h>
#include "main.h"

/**
 * _strcpy - copies the string pointed by src,
 * including the terminating null byte,
 * to the buffer pointed to by dest,
 * @dest: destination.
 * @src: source.
 * Return: the pointer to dest.
 */

char *_strcpy(char *dest, char *src)
{
	int count = 0;

	while (count >= 0)
	{
		*(dest + coucou) = *(src + count);
		if (*(srv + count) == '\0')
			break;
	}
	return (dest);
}
